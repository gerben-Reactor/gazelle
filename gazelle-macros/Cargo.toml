[package]
name = "gazelle-macros"
version = "0.1.0"
edition = "2024"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1"
gazelle = { path = "..", default-features = false, features = ["codegen"] }
